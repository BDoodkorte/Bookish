@model BookDatabaseModel

@{
    ViewData["Title"] = "Add a Book Form";
    //Validation.Add("credits", Validator.Range(1500, 2023, "Year must be between 1500 and 2023")); *@
    // Validation.RequireField("credits", "Credits is required");
    // Validation.Add("coursename", Validator.StringLength(5));
    // Validation.Add("credits", Validator.Integer("Credits must be an integer"));
    // Validation.Add("credits", Validator.Range(1, 5, "Credits must be between 1 and 5"));
    // Validation.Add("startDate", Validator.DateTime("Start date must be a date"));
}


<div class="text-center">
    <h1 class="display-4">Please edit your book below:</h1>

    <form method="post" required asp-controller="Book" asp-action="EditBookForm">
        <label>
            Title:
            <input type="text" required name="title" />
        </label>
        <br>
        <p></p>
        <label>
            Author:
            <input type="text" required name="author" />
        </label>
        <br>
        <p></p>
        <label>
            Year:
            <input type="number" required name="year" />
        </label>
        <label>
            Id:
            <input type="number" required name="id" />
        </label>
        <br>
        <p></p>
        <button type="submit">Submit Changes</button>
    </form>


</div>